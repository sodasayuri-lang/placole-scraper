{
  "name": "placole-scraper",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "render-build": "pup-install && npm install",
    "postinstall": "puppeteer browsers install chrome"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^21.5.0"
  }
}
